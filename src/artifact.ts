import {compare,ENGINE} from './connected.ts';
import {summaryPlan} from './cloudflare-summary.ts';
export function artifact(original:unknown,candidate:unknown){
 const originalBytes=typeof original==='string'?original:JSON.stringify(original,null,2);const candidateBytes=typeof candidate==='string'?candidate:JSON.stringify(candidate,null,2);original=JSON.parse(originalBytes);candidate=JSON.parse(candidateBytes);
 const evidence={originalBytes,candidateBytes,version:1,engine:ENGINE,scope:'Synthetic fixture; no live GTM access',comparison:compare(original,candidate),original,candidate};
 const cv=(original as any).containerVersion;
 const storage=summaryPlan({clientId:'synthetic-demo',accountId:String(cv.accountId),containerId:String(cv.containerId),name:'Synthetic demo'},'demo-'+Date.now(),original,candidate);
 const json=JSON.stringify({...evidence,storage}).replace(/</g,'\\u003c');
 return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>GTM Autoresearch · Jordaaan</title><style>body{background:#0c0b09;color:#f0ece4;font:16px Inter,system-ui;max-width:1100px;margin:auto;padding:24px}h1,h2,button{font-family:monospace}a,b{color:#F5D623}button,select{background:#1a1814;color:#f0ece4;border:1px solid #8B7A12;padding:12px;border-radius:8px}section{padding:20px;background:#1a1814;margin:16px 0;border-radius:12px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:12px}td,th{text-align:left;padding:10px;border-bottom:1px solid #2a2520}textarea{width:95%;min-height:100px;background:#141210;color:#f0ece4;padding:12px}@media(max-width:650px){.grid{grid-template-columns:1fr}}</style><header>ORGANIZED <b>AI</b> · <a href="https://www.linkedin.com/in/jordaaanhill">JORDAAAN</a></header><h1>GTM Autoresearch</h1><p>Synthetic fixture · Actual evaluator results · No container changes applied</p><section><h2>Client and container</h2><p id="target"></p><label>History backend <select id="backend"><option value="cloudflare">Cloudflare D1</option><option value="chumbo">Chumbo / Supabase</option></select></label><p id="connection">Not connected in this standalone preview.</p></section><section><h2>Version comparison</h2><p id="result"></p><table><thead><tr><th>Dimension</th><th>Original</th><th>Candidate</th></tr></thead><tbody id="dimensions"></tbody></table><p>Heuristic quality scores; not live tag-firing tests or measured business improvement.</p></section><section><h2>Findings</h2><div id="findings"></div></section><section><h2>Container evidence</h2><div class="grid"><div><h3>Original</h3><pre id="original"></pre></div><div><h3>Candidate</h3><pre id="candidate"></pre></div></div></section><section><h2>Download container JSON</h2><label>Version <select id="download-version"><option value="original">Original container</option><option value="candidate">Candidate — requires review</option></select></label> <button id="download-json">Download JSON</button> <a id="gtm-import" href="https://tagmanager.google.com/" target="_blank" rel="noopener noreferrer" style="display:inline-block;padding:12px;border:1px solid #8B7A12;border-radius:8px">Open GTM to import ↗</a><p>Open the matching container’s import screen, then choose the downloaded JSON. If container IDs are unavailable, use Admin → Import Container in GTM. Review the destination workspace and Merge/Overwrite preview before confirming. Importing is separate from publishing.</p><p id="download-status" role="status">Exported bytes are the selected container evidence. Downloading does not approve, import, or publish it.</p><p>If Claude blocks the download, ask the assistant to attach the selected container JSON as a file.</p></section><section><h2>Next action</h2><button id="save">Save QA summary request</button> <button id="full-save">Save full container request</button> <button id="review">Prepare review request</button><p id="status" role="status">No request prepared. Nothing saved or applied.</p><textarea id="request" readonly aria-label="Assistant request"></textarea><p>Full container storage currently supports up to 20 KB per version through the file-backed Cloudflare API tool; larger exports are rejected. The Cloudflare connector summary path does not back up container files. Copy the request into your assistant with this HTML file attached. The assistant must verify backend access and exact saved evidence before reporting success. Selecting a backend does not connect it.</p></section><script type="application/json" id="evidence">${json}</script><script>const d=JSON.parse(document.getElementById('evidence').textContent),c=d.comparison;const text=(id,v)=>document.getElementById(id).textContent=v;const cv=d.original.containerVersion;if(/^[0-9]+$/.test(String(cv.accountId||''))&&/^[0-9]+$/.test(String(cv.containerId||''))){document.getElementById('gtm-import').href='https://tagmanager.google.com/#/admin/accounts/'+cv.accountId+'/containers/'+cv.containerId+'/import';}text('target','Demo client · Account '+(cv.accountId||'unspecified')+' · Container '+(cv.containerId||'unspecified')+' · Engine '+d.engine);text('result',c.status+' · '+c.before.combinedScore+' → '+c.after.combinedScore);for(const x of c.before.dimensions){const row=document.createElement('tr');for(const v of [x.name,x.score,c.after.dimensions.find(y=>y.name===x.name)?.score]){const td=document.createElement('td');td.textContent=String(v);row.append(td)}document.getElementById('dimensions').append(row)}for(const x of c.after.issues){const p=document.createElement('p');p.textContent=JSON.stringify(x);document.getElementById('findings').append(p)}text('original',JSON.stringify(d.original,null,2));text('candidate',JSON.stringify(d.candidate,null,2));function prepare(action){const backend=document.getElementById('backend').value;document.getElementById('request').value=action+' for the synthetic GTM Autoresearch evidence embedded in the attached HTML. Backend: '+backend+'. For Cloudflare, read storage.setupSql, storage.writeSql, and storage.readSql directly from the embedded evidence; execute them in that order in the user-selected D1 database. Compare the actual returned summary_json byte-for-byte to storage.json, verify its SHA-256 equals storage.sha256, and check client/account/container/run identity. Only then report Saved and verified. This saves a compact QA summary, not restorable container files. For Chumbo, use its supported adapter or report unavailable. Read the evidence file directly. Confirm client and container identity. Do not modify or publish GTM. If saving is supported, save a new immutable record and verify exact readback; otherwise report the missing adapter. Do not claim this preview is connected.';text('status','Request prepared — nothing sent, saved, or applied.')}document.getElementById('save').onclick=()=>prepare('Save history');document.getElementById('full-save').onclick=()=>{document.getElementById('request').value='Save BOTH complete original and candidate container JSON from this artifact using the file-backed scripts/container-store.ts API transfer. Preserve originalBytes and candidateBytes exactly. Resolve the authorized client/account/container first. Verify both hashes and restore both files before reporting success. This path currently supports at most 20000 UTF-8 bytes per version; never truncate. If this chat cannot run the authenticated file-backed API tool, report that limitation; do not substitute a summary or transcribe encoded payloads. No GTM import or publish.';text('status','Full container request prepared; not saved. Requires file-backed Cloudflare API tool.');};document.getElementById('review').onclick=()=>prepare('Review candidate');document.getElementById('download-json').onclick=()=>{const version=document.getElementById('download-version').value;const selected=version==='candidate'?d.candidate:d.original;const id=String(selected.containerVersion?.containerId||'unknown').replace(/[^a-zA-Z0-9_-]/g,'_');const blob=new Blob([version==='candidate'?d.candidateBytes:d.originalBytes],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='gtm-'+id+'-'+version+'.json';document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),60000);text('download-status','Download requested: '+version+'. Not imported or published. If no file appears, ask Claude to attach this selected JSON.');};</script></html>`;
}
