import {readFile,writeFile,mkdir} from 'node:fs/promises';import {summaryPlan} from '../src/cloudflare-summary.ts';
const original=JSON.parse(await readFile(new URL('../tests/fixtures/container.json',import.meta.url),'utf8'));const candidate=structuredClone(original);candidate.containerVersion.tag[0].name='GA4 - purchase';const cv=original.containerVersion;const p=summaryPlan({clientId:'synthetic-workshop',accountId:String(cv.accountId),containerId:String(cv.containerId),name:'Synthetic fixture'},'gtm-summary-'+Date.now(),original,candidate);const dir=process.argv[2]??'verification/gtm-summary';await mkdir(dir,{recursive:true});await writeFile(dir+'/plan.json',JSON.stringify(p,null,2));await writeFile(dir+'/save.sql',p.setupSql+'\n'+p.writeSql+'\n'+p.readSql);console.log(JSON.stringify({dir,runId:p.summary.runId,bytes:Buffer.byteLength(p.json),sha256:p.sha256}));
