# GTM Autoresearch through Chumbo

Use the standalone twelve-dimension engine, not the smaller audit runtime.

1. Extract the existing evaluator and mutation validation into shared, import-safe modules without changing scores.
2. Add authenticated tools to score supplied complete exports and evaluate candidates against stored evidence with the same ads snapshot. Claude proposes candidates; the service runs deterministic checks. No CLI process or GTM publishing in the service.
3. Save immutable records and owner-isolated review decisions in Supabase, using the existing Chumbo connection and Cloudflare proxy pattern.
4. Present dimensions, before/after evidence and actionable findings in the chosen Organized AI charcoal/gold layout.
5. Verify evaluator parity, malformed input rejection, candidate gates, user isolation, transport, database, build and local deployment. Report hosted Claude sign-in separately.

Remote execution uses uploaded JSON, never account credentials embedded in an artifact. The original CLI remains available.
